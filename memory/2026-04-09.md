# 2026-04-09 工作日志

## 23:00-13:00 Kimi 写 PRD + 飞书排版 + 搭话板块

### 完成事项

1. **Kimi K2.5 直接 API 调用**
   - 发现 Moonshot API 可用，key: `sk-QQhimUcQf4rdmZ9vRCyFLDP5jRmlW4OXnDJBOXa6dLhaBGG2`
   - 关键踩坑：`temperature` 必须为 `1`，其他值报 400
   - `max_tokens` 设 16000 可用
   - Kimi 写内容质量很好：场景化描述、Q&A、设计决策记录

2. **飞书 DOC Better Skill 更新**
   - 路径：`skills/feishu-doc-better/`
   - SKILL.md 更新：加入 Kimi 直接调用方法、排版增强策略、执行流程
   - 脚本保存：`scripts/kimi_write.py`

3. **伴播面板 PRD v2.0 写入飞书**
   - 文档：`GJTKdI5RCopD5ox1Jgqccv2unPg`（386 blocks）
   - 权限：已开链接分享（外部访问 + anyone_editable）
   - 对比：v1 170 Block（我写）vs v2 386 Block（Kimi写+我排版）
   - 排版亮点：Quote引言、Table对比、Code布局图、Ordered流程

4. **搭话板块尝试恢复**
   - CanvasPanel 中添加搭话板块（固定搭话+智能搭话+频率+TTS+AI话术开关）
   - Boss 反馈：恢复的不是截图原样
   - 千问 Vision 对长图只读前1/3，无法准确分析截图
   - 最终：移除搭话板块，待 Boss 提供更清晰需求

### 踩坑
- **千问 Vision 识别长图能力差**：1318x450 的截图只读到前1/3，下面的搭话部分完全漏掉
- **sub-agent 调 Kimi/GLM 会超时**：复杂写作任务不能用 sub-agent，必须直接 API 调用
- **sub-agent 温度参数**：Kimi 仅支持 temperature=1

### 涉及文件
- `src/CanvasPanel.tsx`：搭话板块添加后又移除
- `skills/feishu-doc-better/SKILL.md`：更新
- `skills/feishu-doc-better/scripts/kimi_write.py`：新增
- 飞书文档 `GJTKdI5RCopD5ox1Jgqccv2unPg`：新建
- 2026-04-09T05:16:11.646Z /new → session=agent:miijia2:feishu:miijia:direct:ou_89aa7a5927040f6036472e6482933b9a source=webchat

---

## 14:51-15:08 声控切镜分析 + image 工具修复

### 完成事项

1. **声控切镜技能点分析**
   - Boss 发原型截图，要求分析「技能点」下四项逻辑
   - 用 qwen-vision OCR 提取全部文字（准确度高，完整提取）
   - 内置 image 工具 403 失败（底层 anthropic/claude-opus-4-6 已下线）
   - 结合 memory/voice-switch-logic-v2.md 发飞书卡片汇报（5张卡）

2. **关键发现**
   - 展示是「自动触发」：讲解中商品切换时系统自动退旧+入场新，不是声控口令
   - 直切含状态迁移：不仅切形象，还自动把「讲解中」状态挪到对应商品
   - 进场依赖前置条件：讲解中商品需已绑定形象

3. **内置 image 工具修复**
   - 查 OpenClaw 文档确认：agents.defaults.imageModel 控制 image 工具底层模型
   - 改动1：bailian/qwen3.5-plus 的 input 从 text 改为 text+image
   - 改动2：agents.defaults 新增 imageModel: bailian/qwen3.5-plus
   - Gateway 重启后验证通过，image 工具正常返回

4. **飞书卡片规范纠正**
   - Boss 指出卡片格式不对（我自定义 JSON），应该用 report.json 模板
   - 重新用 report.json 模板结构发了第二版卡片（5张）
- 2026-04-09T07:12:16.071Z /new → session=agent:miijia2:feishu:miijia:direct:ou_89aa7a5927040f6036472e6482933b9a source=webchat

---

## 15:12-15:54 模型切换 + 飞书卡片规范 + 技能组功能落地

### 完成事项

1. **模型切换**
   - 发现当前会话用的是 mimo/mimo-v2-pro 而不是配置的 zai/glm-5.1
   - 原因：会话级模型覆盖生效
   - 解决：用 session_status(model=default) 切回 zai/glm-5.1

2. **飞书卡片规范确认**
   - Boss 指出之前卡片格式不对（自己造 JSON）
   - 确认必须用 feishu-card-composer skill 的模板（report.json 等）
   - 凭证坑：必须用当前 bot 自己的 app_id/app_secret，否则报 cross app 错误
   - 米迦凭证：cli_a92d04f7fe781cd5 / aDkHKtAmtia6LYQWiFVNQh7f2dLLqRhv

3. **声控切镜逻辑总结（卡片版）**
   - 用 report 模板发 5 张卡片
   - 三种口令：进场/退场/指定链接切换（硬切）
   - 核心变化：旧版「展示口令 = 进场 + 换装」，新版拆为三种独立口令

4. **技能组功能方案设计**
   - Boss 发两张示意图，要求分析后提出 UX 方案
   - 用 qwen-vision 分析截图内容（5种场景、关键词触发、随机动作片段）
   - 第一版方案有误（假设在「场景装修」子标签下新增 Tab）
   - Boss 纠正：是左右面板设计，技能组加在右栏「技能点」下方
   - 修正后发 V2 方案卡片（5张），Boss 确认

5. **技能组功能落地**
   - 在 CanvasPanel.tsx 新增数据结构和 UI
   - 5个预设技能组：效果肯定/逼单助攻/使用演示/感谢下单/整活表演
   - 手风琴卡片设计，每组可展开/折叠，Toggle 开关控制启用
   - 关键词 Tag 配置（默认 + 自定义添加/删除）
   - 「感谢下单」组特殊：无关键词，系统自动识别
   - 构建 + 提交 + 部署到 Cloudflare Pages
   - 部署地址：https://9475684f.miaobo-b.pages.dev

### 踩坑
- **Cloudflare 部署中文 commit message 报错**：必须用英文或用 --commit-message 参数覆盖
- **Toggle 组件 props**：是 checked/onChange，不是 value/onChange

### 涉及文件
- `src/CanvasPanel.tsx`：技能组数据结构 + 状态 + UI（+205行）
- commit: 5607c23

---

## 15:54-16:55 技能组功能迭代（命名/引导/空状态/预览弹窗）

### 完成事项

1. **命名优化**
   - 「技能点」→「全局指令」（所有伴播形象通用）
   - 「技能组」→「形象技能」（仅当前伴播形象适用）
   - 引导文字简化：「当主播话术命中关键词时，随机播放组内一个动作片段」

2. **用户引导设计**
   - 标准音质形象：提示「当前形象仅支持播放预制动作片段，无法自定义说话内容」
   - 高级音质形象：提示「此处仅配置动作片段。如需让形象说指定话术，请前往「智能搭话」模块」
   - 与形象信息栏的「🟢标准」/「🔵可搭话」标签呼应

3. **形象 × 技能组动作映射**
   - 新增 AVATAR_SKILL_ACTIONS 数据结构，动作跟随形象
   - 不同形象在不同技能组下可能有/无动作
   - 无动作时：折叠态显示「暂无动作」+ Toggle置灰；展开态显示空状态提示

4. **动作片段预览弹窗**
   - 缩略图 hover 效果：播放图标 + 轻微放大 + 阴影
   - 点击打开 400px 宽预览弹窗
   - 视频有声音：默认音量50%，支持播放/暂停/静音/音量滑块
   - 底部控制栏：⏸播放/暂停、🔇静音、音量滑块、关闭、输出按钮

### 踩坑
- **Toggle 组件需支持 disabled**：给 shared.tsx 的 Toggle 加了 disabled 参数

### 涉及文件
- `src/CanvasPanel.tsx`：VideoPreviewPopup改版 + 动作缩略图hover/click + AVATAR_SKILL_ACTIONS数据
- `src/shared.tsx`：Toggle 组件加 disabled 支持
- commits: 6a0432f, a6f46ea, 623ee8e, 616e717, 9b6beb8

### 最终部署地址
- https://miaobo-b.pages.dev/

---

*会话结束时间：2026-04-09 16:59*
*上下文占用：64%（140k/220k）*
