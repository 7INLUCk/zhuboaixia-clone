# 2026-03-18 工作日志

## 首页桌面端"铺满"讨论
- Boss 反馈桌面端首页两侧有空白，没有随设备全屏铺满
- 方案探讨：A（去掉 max-w）、B（背景铺满+内容居中）、C（分区混合）
- 截了 Spotify/Netflix/TikTok 桌面端参考图发给 Boss
- 先尝试方案 B（改 layout 结构）→ K2.5 改坏了（Hero 文字变竖排），回退
- 改用轻量方案 B'（body 加 bg-[#FAF9F7]，内容容器加 bg-white）→ 改完但颜色太接近看不出差异
- 最终决定：不改了，保持现状

## 趋势页响应式适配（待做）
- Boss 截图显示桌面端趋势页卡片只有单列，浪费空间
- 已读取源码，分析完毕
- 方案：改 VideoCard 的 scale/translate（大屏去掉）+ 卡片容器从 flex-col 改为 grid 多列
- **提示词已给出，Boss 说明天再改**
- 待粘贴的提示词要点：
  1. VideoCard 根 div 加 md:scale-100 md:mb-0 md:translate-x-0
  2. 卡片列表 flex-col → grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3

## 飞书发图修复
- feishu-send-image 脚本的 send 阶段报 400（open_id cross app）
- 根因：脚本默认读 openclaw.json 根级 feishu config（小贾的 app），但米迦用的是 accounts.miijia 的 app
- 临时用内联 python 指定 miijia 的 appId/appSecret 成功发图
- 已给脚本加了 `user:` 前缀支持，但 cross-app 问题需要脚本支持 --account 参数才能根治
