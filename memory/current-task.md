# 当前任务状态

## 任务名：CanvasPanel 两列式融合方案重构（分支 c）

### 进度
- ✅ 已完成：产品页面全览梳理（5张卡片）
- ✅ 已完成：参考图 vs CanvasPanel 对比分析（5张卡片，qwen-vision 分析白板手绘图）
- ✅ 已完成：融合方案 V1（7张卡片，含6项待确认点）
- ✅ 已完成：Boss 逐条确认所有待确认点
- ✅ 已完成：融合方案 V2 最终版（7张卡片）
- ✅ 已完成：Boss 确认 V2 方案 + 命令开工
- ✅ 已完成：git checkout -b c（分支已创建）
- ❌ 未完成：重写 CanvasPanel.tsx 为两列式布局（刚要开始写文件，上下文满了）

### Boss 确认的最终方案（branch c 要实现的）
**整体布局**：左右两列（左列~300px商品列表 + 右列flex伴播配置）

**左列：直播商品列表**
- 商品卡片（宽松版~80-90px高）：复选框 + 缩略图 + 标题+价格 + 状态标签（已绑定/未绑）
- 全选复选框
- 批量操作栏（底部浮出）：勾选N个商品 → 「批量配置伴播」按钮

**右列：伴播配置**
- 形象信息栏 + 「更换伴播」按钮（弹窗保留）
- 9:16 视频预览（ChromaKey抠绿 + 自动轮播常规态）
- 预览按钮行：入场🎬 预览 / 退场🚪 预览（无形象时置灰）
  - 点击 → 弹出视频片段弹窗（大尺寸播放 + 「输出到直播伴侣」按钮）
- 「技能点」标题区（对齐参考图）：
  - 入场：口令输入框（默认「有请模特上场」）
  - 退场：口令输入框（默认「请模特先下场」）
  - 直切：口令输入框（默认「看看{N}号链接的模特上身效果」）
  - 展示：只读「自动触发」（保留，系统行为说明）
- 「形象专属能力」（仅绑定形象后显示）：
  - 特殊动作：名称+时长+开关+触发口令（入场/退场已过滤）
  - 搭话能力（仅🔵可搭话形象）：开关+规则列表
  - 标准形象：显示「该形象不支持搭话」
- ⏰ 常规态时间轴（方案C：折叠，默认收起）
- ⚠️ 形象切换时弹确认弹窗（保留）

**批量配置弹窗（Option A）**
- 选形象 + 入场/退场/直切口令 → 一键应用到选中商品
- 不含特殊动作（形象自带）

### 关键决策记录
1. 参考图是领导最终定的布局方向，两列式
2. 参考图右侧面板里的圆形 = 我们的抠绿视频预览（手绘画不出来）
3. 「讲解中」状态暂不需要展示
4. 入场/退场/直切是全局指令，不限于单商品/单形象
5. 特殊动作和搭话能力是形象专属的
6. 展示=讲解中商品切换时的自动退场入场，保留为只读说明
7. 换装=直切功能（指定链接号直接切形象）
8. 常规态6动作系统自动，无需用户配置
9. 输出到直播伴侣移到视频片段弹窗内
10. 批量应用功能去掉，改为左列批量选择

### 下一步
1. 在分支 c 上重写 CanvasPanel.tsx（两列式布局）
2. 部署到 Cloudflare Pages
3. 给 Boss 验收链接
