# 2026-03-09

- 15:36 在 Boss 同意“可以，去吧”后，准备向网页 Agent 发送 P0 最小改动指令（showcase.getById 补 stageUsername/avatarUrl + 详情页映射回退链调整）。
- 执行 browser.act(type+submit) 时，OpenClaw browser control service 超时（20s），报错提示不要重试 browser 工具。
- 影响：本轮未能把执行指令发出，任务被阻断。
- 已采取动作：准备向 Boss 透明说明浏览器控制通道故障，提供可直接粘贴的指令文本，并请求是否允许我重启 gateway 后继续。
- 15:44 在 Boss 要求“抓返回+下发根因排查提示词”时，再次调用 browser.act 发送指令，仍报相同超时（报错文本仍显示 timed out after 20000ms）。
- 结论：目前 browser 工具调用侧 timeoutMs 参数未生效或未透传到该错误分支，需先走手动粘贴链路保障任务连续。
- 15:55 Boss 要求读取“新一轮回复”后，我先成功 snapshot，但内容仍停留在旧报告；尝试点击“滚动到底部”抓取最新消息时，再次触发 browser 超时（固定 20000ms），导致无法继续自动读取新回帖。
- 17:20 在恢复后再次尝试直接向网页 AI 助手发送“头像样式异常排查提示词”，browser.act 再次触发固定 20000ms 超时，说明连接仍为抖动态（并非稳定恢复）。
- 18:00-18:59 头像失败态修复期间反复出现 Internal Server Error，先后被误判为“头像改动引发 / 数据库P1001引发”；最终通过“代码回滚 + 连续压测 + 运行态重建”确认根因是 Next.js dev server 运行态异常（编译缓存/残留进程）。
- 关键分层结论：
  1) 代码层是否致因，必须先用 `git 状态 + 回滚后同路由压测` 验证；
  2) DB层是否致因，必须区分“Prisma直连成功”与“API路由返回500”；
  3) 当出现“DB直连正常但 API 100% 500”，优先判定为 Next/TRPC 运行态问题而非业务逻辑。
- 最终恢复动作（有效）：清理 `.next` + 终止残留 dev 进程 + 重新生成 Prisma client + 重启编译。
- 经验：以后遇到“改完就炸”不能直接归因功能改动，先跑“分层诊断四步”：代码状态、路由压测、直连数据库、运行态重建。