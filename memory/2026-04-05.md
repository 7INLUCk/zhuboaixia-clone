# 2026-04-05 工作日志

## 上午（11:00-12:00）
- Boss要求汇报伴播前端+方案B进度，用飞书卡片发送
- 一轮改造：
  - 助播Tab从8个改为9个（声控切品/搭话助播/导播台/发评回评/声控开价预热/欢迎感谢/发优惠券/发福袋/系统设置）
  - 按钮文字「开始直播」→「启动助播虾」
  - 面板宽度640→800→740→710px（多轮调整，Boss最终确认710）
- Git: 已commit+tag `pre-renovation-2026-04-05`

## 下午（12:40-17:00）
- Boss发来两个文档要求分析：
  1. 抖音童装直播-AI伴播SOP（feishu_doc成功读取，token: T7Qwds4ktoLCP8xH9j1cbBfsnVb）
  2. 织里童装落地方案对齐会议纪要（Boss粘贴，跨组织API 403读不了）
- 输出综合改造方案（6模块改造+3新增功能）
- Boss三点意见：
  1. 音色Tab可以去掉（已锁定不可改）
  2. 伴播形象分层：形象+造型融合为一个平级选项，下分常规态和事件态
  3. 左侧预览应改为视频预览
- 二轮改造开始：
  - Tab精简4→3（商品伴播/进退场指令/系统设置）
  - 类型和路由已更新
  - ProductAvatarContent 需要重写（未完成，被多次 session 重置打断）

## 傍晚（18:00-18:40）— 本轮会话

### 根因分析：MiiMii 卡死问题
- **根因确认**：不是 mimo 模型不稳定，是 workspace 注入上下文太大（~80KB vs Alex 的 ~43KB）
- MEMORY.md 45KB + AGENTS.md 20KB + 其他文件 → 读大文件时请求 payload 过大，mimo API 连接断开
- Alex 没问题因为他注入量只有一半
- **修复**：MEMORY.md 从 45KB 精简到 6KB（归档到 `memory/archive/MEMORY-full-2026-04-05.md`），删除 BOOTSTRAP.md，总注入 79KB → 33KB

### ProductAvatarContent 三级结构 UI 完成
- 组件重写：商品 → 伴播形象 → {常规态, 事件态} 带视频预览卡片
- hover 自动播放视频 + duration 角标
- SystemSettingsContent 占位组件已建
- Git: `40cd2d8 checkpoint: ProductAvatarContent三级结构+SystemSettingsContent占位`

### UX 审查 + 重新设计
- 截图分析 + qwen-vision 对两个 Tab 的 UI/UX 分析
- 7 个关键问题：信息过载、无引导、视觉层级混乱、Tab 语义不清、详情页可读性差、底部功能区不明
- Boss 核心需求：左侧预览区改为视频时间轴

### 伴播面板 UX 重做（已部署）
- **左侧预览**：静态照片 → 视频时间轴预览（每个形象一行 + 🔵常规/🟠事件时间条）
- **Tab 改名**：形象配置 / 动作设置 / 高级设置（带描述文字）
- **右侧面板**：去商品信息重复、常规态→「直播中（自动循环）」、事件态→「活动触发（指定场景播放）」
- **首次引导**：🚀 快速开始 3 步引导卡片
- **底部按钮**：「重置」+「💾 保存配置」，权重提升
- Git: `9cad07c feat: 伴播面板UX重做 - 时间轴预览+Tab改名+首次引导+去信息重复`
- 部署: https://miaobo-b.pages.dev

### Git 提交记录
```
9cad07c feat: 伴播面板UX重做 - 时间轴预览+Tab改名+首次引导+去信息重复
40cd2d8 checkpoint: ProductAvatarContent三级结构+SystemSettingsContent占位
999bda6 feat: 伴播Tab改造-商品伴播管理+进退场指令组件
```

### 待继续（下个会话）
- [ ] Boss 验收新设计，根据反馈调整
- [ ] 废弃组件清理（VoiceContent / AutoChatContent / VoiceSwitchContent）
- [ ] SystemSettingsContent 内容实现（版本分级+NDI+设备状态）
- [ ] 底部侧边按钮（🐟伴播/📋KT板/⚙️设置/📱设备）整合到 Tab

### 教训
- 被多次 session 重置打断，同一个替换任务跨越了4个session没完成
- **根因**：MEMORY.md 45KB 太大，读大文件（CanvasPanel 2000行）时 mimo API 超时
- **预防**：MEMORY.md 保持精简（<20KB），读大文件用 offset/limit 分段
- qwen-vision 提示词太长会超时，保持简洁

## 傍晚续（18:42-19:00）— 第二轮改造

### 商品绑定一对一改造（Issue #1）
- Boss 发截图 + 口述新逻辑：每个商品只能绑定一个形象（一对一）
- 概念澄清：商品列表（系统同步）→ 用户手动绑定形象 → 常规态6状态随机轮播 + 事件态口令触发
- GitHub: 创建 Issue #1 + 分支 `codex/issue-1-product-avatar-binding`
- 数据模型重构：`avatars: AvatarOption[]` → `boundAvatarId: string | null`
- 新增：AVATAR_LIBRARY（7个形象）、NORMAL_STATES（每形象6个）、EVENT_ACTIONS（每形象N个口令）
- ProductAvatarContent 重写：三栏布局（商品列表200px + 视频预览 + 形象库180px）
- TimelinePreview 适配一对一绑定模型
- 部署验证：商品列表显示绑定状态，点击商品预览6个常规态+事件态口令
- Git: 5d64aff, 已推送到远程分支

### 待继续
- [ ] Boss 验收第二轮改造
- [ ] 事件态口令配置区域（是否需要单独做）
- [ ] PR 合并
- [ ] 废弃组件清理
- [ ] SystemSettingsContent 实现

## 下午-晚上（13:00-19:26）

### 两栏布局改造（已完成但被 Boss 推翻）
- 布局溢出修复：缩窄列宽 + min-width:0（ce5abfb）
- 砍掉 TimelinePreview 组件 (~200行)，改两栏布局：商品列表180px + 右栏纵向排列
- 已部署+验证效果，Git: 0a2205d

### Boss 观感（19:24）
- Boss 认为两栏效果「有点一般」
- **新方案**：把 Timeline 拿回来，做左栏固定在面板最左侧
  - Timeline 商品列表扩展为「显示全部商品」（消除与形象配置 Tab 的重复）
  - 形象配置 Tab 删掉自己的商品列表，统一由 Timeline 承载
  - Timeline 不随 Tab 切换消失，永远可见
- 已发5张方案卡片，等 Boss 确认三个问题：
  1. Timeline 宽度 240px OK？
  2. Tab 名字要不要改？
  3. 事件态口令配置放哪个 Tab？

### 待继续
- [ ] Boss 确认方案后执行 Timeline 回归改造
- [ ] 事件态口令配置区域
- [ ] PR 合并
- [ ] 废弃组件清理
- [ ] SystemSettingsContent 实现

## 晚上（19:26）Boss 纠正绑定规则
- **不强制绑定**：允许某些商品不绑定，但要有明确橙色提示防漏绑
- **多对多绑定**：一个形象可绑定多个商品，形象库无锁定
- 重新发了6张方案卡片，等 Boss 确认4个问题

## 晚上（19:29）Boss 确认方案 ✅
- 4个问题全部确认：Timeline 240px OK、Tab名字不改、事件态口令放动作设置Tab、未绑定提醒OK
- 方案存档：current-task.md，下个会话执行
- 核心改动：Timeline回归左栏固定 + 形象配置Tab去掉商品列表 + 动作设置Tab加事件态口令 + 多对多绑定不锁定

## 晚上（19:30-19:51）— Issue #2 Timeline回归改造执行

### Issue #2 创建 + 分支
- GitHub Issue #2: `[任务] 伴播面板Timeline回归左栏+商品列表合并+多对多绑定`
- 分支: `codex/issue-2-timeline-regression`（基于 `b` 分支，从 issue-1 带入已有改动）
- 已认领

### 代码改动
- **新增 TimelineLeftBar 组件**：240px 固定左栏，含商品列表（绑定状态）+ 视频预览 + 🔵常规/🟠事件时间条 + 开启伴播按钮
- **重写 ProductAvatarContent**：改为接收 props（selectedProductId/products/setProducts 等），去掉左栏商品列表，只保留右侧内容
  - 多对多绑定：去掉 `boundToOther` 灰化逻辑，形象库无锁定
  - 形象库显示"绑定X个"计数
- **CommandContent 新增事件态口令区**：按形象分组展示 EVENT_ACTIONS
- **主面板 banbo 区域重构**：lift state 到 Panel 函数 → TimelineLeftBar + Tab 内容区并排
- **Tab 内容**：形象配置（ProductAvatarContent）/ 动作设置（CommandContent）/ 高级设置（SystemSettingsContent）

### 部署踩坑
- Vite 生产构建 hash 不变 → Cloudflare 不上传新文件
- **根因**：Vite 内容哈希确定性，相同大小的输出产生相同 hash
- **解法**：修改源代码文本（如加 "V2"）→ 改变输出大小 → hash 变化 → Cloudflare 上传
- 部署多次（5次）才完成最终版本
- 修复了一个 bug：未绑定商品双 ⚠ 显示

### Git 提交
- `b929d47` feat: Timeline回归左栏+多对多绑定+事件态口令 Closes #2
- 已 push 到远程分支

### 待继续
- [ ] PR 创建 + 合并
- [ ] 废弃组件清理（VoiceContent / AutoChatContent / VoiceSwitchContent）
- [ ] SystemSettingsContent 实现（版本分级+NDI+设备状态）
- [ ] 底部侧边按钮整合

## 晚上（20:10-20:20）— Timeline 样式修正 + 方案讨论

### Timeline 样式问题
- Boss 指出新 Timeline 样式不对，要求复用旧版（commit 9cad07c 的 TimelinePreview）
- 旧版特征：深色背景 #111827、280px、每个形象一张卡片、9:16视频预览、LIVE标签、可视化时间轴条（蓝色交替段+橙色单段+触发点标注）
- 我做的版本是白底浅色风格，差距大

### 修复 commit
- `647937e` fix: Timeline恢复深色风格+可视化时间轴条（复用9cad07c样式）
- TimelineLeftBar 完全重写：深色背景、per-avatar 卡片、视频预览区、时间轴条可视化
- 已 push + 部署

### 整体样式讨论 → 方案C已落地
Boss 选择方案 C（渐变过渡带，低成本）
- `44e22eb` style: Timeline右侧渐变过渡带（方案C-柔化深浅交界）
- Timeline 右边缘: `box-shadow: 20px 0 30px -10px rgba(0,0,0,0.25)`
- 配置区左边缘: `inset 8px 0 16px -8px rgba(0,0,0,0.06)`
- 效果：深→白硬切改为柔化渐变

### 整体样式讨论（Boss 待确认）
Boss 提出：左边深色 + 右边白色有违和感
发了 5 张飞书卡片（分析 + 3 方案 + 建议）：
- **方案 A**（推荐）：右侧改深灰底，统一暗调（像剪映/Premiere）
- **方案 B**：右侧改暖灰底，过渡缓冲
- **方案 C**：交界处加渐变过渡带

### Vite 构建 hash 问题（重要教训）
- Vite 内容哈希确定性 → 相同大小输出产生相同 hash → Cloudflare 不上传
- 解法：修改源码文本改变输出大小 → hash 变化 → 上传
- 影响了整个 session 的部署效率（5次部署才成功）

## 晚上（20:26-20:42）— 伴播触发机制设计

### 重读飞书文档
- Boss 提问是否记得两个飞书文档（SOP + 会议纪要）
- 重新读取了「抖音童装直播-AI伴播SOP」（feishu_doc token: T7Qwds4ktoLCP8xH9j1cbBfsnVb）
- 会议纪要（织里童装落地）是 Boss 手动粘贴的，跨组织 API 403，原文在多次 session 重置后丢失

### 伴播状态机逻辑梳理
- 读取了 Boss 发的两张 SOP 截图（qwen-vision）
- 整理了 3 大场景：口令进退场、口令换装、自动展示动作
- 异常兜底：无对应形象→画面不变+提示、无伴播时退场→提示无需退场

### 🔴 关键设计决策：切品 × 伴播形象
- Boss 发现漏洞：切品时不会自动切换伴播形象
- v1 方案：切品自动联动（切品时伴播在场→换装，不在场→不变）
- **Boss 确认 v2 方案**：没有独立「切品」机制，口令即切品
  - 口令「看N号上身效果」→ 不在场=入场动画进场 / 在场=原地换装
  - 口令「请小朋友下场」→ 退场
  - 全程只有口令触发，无自动联动

### 动作设置面板改造建议
- 现状3个口令（进场/换装/退场）→ 改后2个（展示/退场）
- 进场和换装合并为「展示口令」（因为本质是同一个动作，只是状态不同）
- 待 Boss 确认后执行改造

### 待继续
- [ ] Boss 确认面板改造方案
- [ ] PR 创建 + 合并（codex/issue-2-timeline-regression）
- [ ] 废弃组件清理
- [ ] SystemSettingsContent 实现
- [ ] 动作设置面板改造（2口令版）

### 本会话全部 commit（codex/issue-2-timeline-regression）
1. `b929d47` feat: Timeline回归左栏+多对多绑定+事件态口令 Closes #2
2. `647937e` fix: Timeline恢复深色风格+可视化时间轴条（复用9cad07c样式）
3. `44e22eb` style: Timeline右侧渐变过渡带（方案C-柔化深浅交界）

### Vite hash 缓存问题
- 修改源码文本改变输出大小 → hash 变化 → Cloudflare 上传新文件
- 这个 session 花了多次部署才解决此问题

### 下个会话继续
- PR 创建 + 合并
- 废弃组件清理
- SystemSettingsContent 实现
- 底部侧边按钮整合
