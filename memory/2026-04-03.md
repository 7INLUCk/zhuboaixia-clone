# 2026-04-03 工作日志

## 会话记录

### 16:30-16:40 PRD 文档复习（方案 A）
- Boss 要求复习方案 A 的整体用户路径和功能设计
- 从 MEMORY.md 回忆了方案 A 的核心架构（形象三级架构、动作=录播、音色锁定等）
- 阅读 PRD 文档（11张图片）：
  - 产品定位：AI 伴播形象与真人主播同屏互动
  - 用户路径：入口 → 创建直播间 → 配置面板 → 启动伴播
  - 10大模块：核心配置（5个）+ 直播工具（6个）
  - 形象三级架构：形象主体 → 造型 → 默认状态&动作
  - 音色锁定：动作素材配置后自动锁定音色
  - 场景装修：默认带入主播占位 + 伴播形象（不可删除）
- 发送多张飞书卡片汇报复习结果

### 18:49 第一次存档
- Boss 要求存档当前会话
- 发送存档卡片确认

### 14:32 第二次存档（当前）
- Boss 再次要求存档当前会话
- 执行存档操作

### 14:58 Boss 纠正：卡片规则 + 搜索规则
- **纠正1**：问候回复3句话，没走卡片 → 违规
- **纠正2**：搜索必须 Tavily + Brave Search 双重验证
- **15:03 Boss 确认**：允许使用内置 web_search 作为第二搜索源
- **已更新**：SOUL.md + AGENTS.md 搜索规则，改为 Tavily + Brave Search 双重验证

### 15:03 分支 B 伴播用户链路复述
- Boss 要求回忆分支 B 的伴播原型用户链路
- 已读取 App.tsx / BanboHomePage.tsx / BanboPanelPage.tsx / BanboCreatePage.tsx / SetupWizard.tsx
- 完整链路：管理广场 → 创建向导（3步）→ 控制台（抖音中控台+伴播面板5个Tab）

### 15:05 方案 B 部署到 Cloudflare Pages
- 创建新项目 `miaobo-b`，production branch = `b`
- 构建 + 部署成功，线上地址：https://miaobo-b.pages.dev
- 部署脚本：`deploy-b.sh`（与方案 A 的 deploy.sh 独立）
- 两个方案互不影响：
  - 方案 A：`miaobo-prototype.pages.dev`（backup/snapshot-2026-04-02）
  - 方案 B：`miaobo-b.pages.dev`（b 分支）

### 15:21-15:37 方案 B 默认页面改为中控台
- Boss 要求：打开方案 B 原型直接看到中控台覆盖层（BuyinDashboardOverlay），跳过切模式/进广场/点卡片三步
- 确认过程：3 轮确认（意图→精确流程→确认执行）
- 代码改动：App.tsx 中 `showBuyinOverlay` 从 `false` 改为 `true`
- 已部署成功，线上地址：https://miaobo-b.pages.dev
- 原有页面全部保留，未删未改

### 15:38 存档
- Boss 要求存档当前会话

---

## 今日完成

1. ✅ PRD 文档复习（方案 A 完整设计）
2. ✅ 方案 A 用户路径梳理
3. ✅ 10大配置模块复习
4. ⚠️ 会话启动问候违规（3句未走卡片），Boss 纠正

---

## 待办

- 无（等待 Boss 新指令）

---

## 关键上下文

### PRD 文档
- 链接：https://vqz9o07xyt.feishu.cn/docx/LYJgdNgxFoFVITxDaqOc1gw9nNb
- 版本：v1.0（2026-03-31，Leo）
- 图片：11张
- 核心内容已录入 MEMORY.md

### 原型分支状态
- `backup/snapshot-2026-04-02` — Production Branch，线上锁定
- `b` — 新分支，用于后续改动
- 线上地址：https://miaobo-prototype.pages.dev

### 16:05-16:30 AGENTS.md/SOUL.md 整理 + 设计 Skills 安装
- Boss 要求确认飞书卡片规则是否生效
- 发现 AGENTS.md 有两套矛盾搜索规则，SOUL.md 有卡片执行规则混杂
- **执行**：
  1. 飞书卡片规则从 SOUL.md 迁移到 AGENTS.md，统一引用 feishu-card-composer skill
  2. 删除 AGENTS.md 中与"强制联网搜索"矛盾的旧版"绝对禁止 web_search"
  3. SOUL.md 精简为只保留身份/原则
- Boss 要求搜索专业产品设计知识 + ClawHub 高星 skills
- **搜索结果**：Tavily + Brave 双重验证，ClawHub 搜索
- **安装 6 个设计 Skills**：
  - superdesign（现代 UI 设计指南）
  - ui-ux-design（Mobile-first + WCAG 2.2 + Tailwind）
  - accessibility（WCAG 2.1 AA 合规）
  - ui-ux-pro-max-plus（50+设计风格 + 100+配色 + 字体配对）
  - web-design（CSS 实现模式）
  - frontend-design-pro（中文设计规范 + 反模式清单）
- AGENTS.md 更新"设计 Skills 自动触发"规则，替代旧的 impeccable 单一配置
- 统一铁律：OKLCH / 禁止Inter / 4px间距 / 4.5:1对比度 / Mobile-first / 无障碍合规
## 17:37 - ConfigPanel 返工 + 复刻

### 事件
- Boss 发助播虾配置面板截图，要求先复刻面板 UI 再加伴播功能
- 我的第一版（子 agent）没复刻面板，只在 9:16 画布上加了个 toggle
- Boss 纠正，我出新方案（新建 ConfigPanel.tsx 复刻截图 + 伴播开关）
- Boss 说「执行执行执行」
- 子 agent 建了 ConfigPanel.tsx，已部署

### ConfigPanel.tsx 内容
- 标题栏「助播虾配置面板」
- 形象/配音按钮 + 🐟 伴播开关
- 8 Tab（直播/话术/.../挂件）
- 直播 Tab 7 个 toggle
- 智能回复展开态（10 个子项 grid）
- 伴播开启 → 右侧滑出 4 Tab 面板

### 教训
- 给子 agent 指令必须：①精确 UI 描述 ②截图路径 ③执行顺序
- 已写入 LEARNINGS.md

### Git commit
- `0c080f5` feat: 新建 ConfigPanel 复刻助播虾配置面板 + 伴播开关

### 待办
- Boss 验收 ConfigPanel，可能需要调面板宽度/按钮样式
- 验收通过后可删旧 Banbo 文件（BanboHomePage/SetupWizard 等）

## 19:30-20:02 TMK LIVE 背景图还原 + 9:16 画布组件还原

### 事件
1. Boss 要求以 TMK LIVE 背景图做 9:16 画布 + 配置面板
2. 我先让子 agent 分析原图 UI 元素，子 agent 严重误判（认成智能家居）
3. 我自己分析后也有误判（上传按钮/方向键/品牌文字等不准确）
4. MiMo-V2-Omni 视觉分析也严重幻觉（把直播间背景认成智能家居面板）
5. **Boss 纠正**：不要猜了，直接告诉我是什么 → Boss 发参考图 + 明确按钮清单

### Boss 最终明确（参考图）
右侧 4 个按钮（从上到下）：
1. 伴播（🐟 图标 + toggle 开关）
2. KT板
3. 设置
4. 设备

底部：状态「未开启」+ 「开始直播」绿色按钮

### 执行
- 新建 `CanvasPanel.tsx`：9:16 画布（TMK LIVE 背景）+ 右侧 4 按钮 + 底部状态栏 + 伴播配置面板
- 修改 `BuyinDashboardOverlay.tsx`：CanvasPanel 替代 BuyinControlPanel 作为 'main' 面板
- 伴播开关：开启→右侧滑出 4 Tab 配置面板（形象库/音色/智能对话/声控切屏）
- 已部署 https://miaobo-b.pages.dev

### 教训
- **视觉分析能力极度有限**：MiMo-V2-Omni 对直播场景严重幻觉，自己辨认中文小字也不准确
- **正确做法**：让 Boss 直接给出准确的 UI 元素清单，不要猜
- 已写入 LEARNINGS.md

## 20:20-21:50 配置面板架构重构：助播/伴播双模式

### 需求确认
- Boss 发两张截图：助播虾功能区 Tab 设计 + 左侧 canvas 右侧配置的伴播界面
- 我发 6 张 AI 生图给 Boss 选方案 → Boss 选"同容器，按 Tab 切换"
- Boss 明确：原先助播面板的按钮（形象/配音/贴片）是个别区域+子 Tab 切换

### Boss 的明确决策（关键）
- **助播 = 场外中控**：无形象预览，有场景装修，有营销工具（福袋/优惠券等）
- **伴播 = 有形象的透明通道**：输出 Alpha 通道透明底视频，无营销工具
- **子 Tab 覆盖原先助播面板内 Tab**，点击修改后覆盖原始面板
- 子 Tab（轮播/场景/贴品/ChromaKey/声音/音色/主播站位/声控切品）更符合业务
- 左边有 canvas preview 体验一致，需实现左边缩略 canvas 实际效果

### 执行
- 修改 `src/CanvasPanel.tsx`（唯一改动文件）
- 顶部大 Tab：「🎤 助播」|「🐟 伴播」切换
- 助播模式：8 子 Tab + 占位内容 + 开始直播按钮
- 伴播模式：左侧 280px 画布 + 右侧 4 Tab 配置区
- 部署成功 https://miaobo-b.pages.dev

### 视觉优化（第二轮）
用 qwen-vision 分析（超时）+ image 工具（403）→ 自行审查：
- 子 Tab 字号 12→13，padding 加大，指示线 2→2.5px
- 画布宽度 240→280px，按钮 32→36px
- Alpha 说明字号 9→10，行高加大
- 底部按钮 padding 8→10，字号 13→14

### 待继续
- Boss 验收两个模式的视觉效果
- 子 Tab 功能区逐步开发（接入实际组件）
- 伴播模式 4 Tab 功能区逐步开发

### 教训
- qwen-vision skill 本地图片超时（可能图片太大或网络问题），需排查
- image 工具 OAuth 403（Claude API 权限问题），不可用
- 自行视觉审查可替代 AI 分析

## 21:49 强制存档（当前）
- 上下文即将压缩，执行存档
- 更新 current-task.md + MEMORY.md + 当日日志
- 发送飞书通知

## 22:01-22:21 视觉分析（Qwen Vision）+ 面板宽度讨论

### Qwen Vision 分析（成功）
- qwen-vision skill 默认 timeout 60s 太短，手动改为 180s 后成功
- **助播模式分析结论**：布局层级混乱、Tab 间距尚可但蓝线过短、底部绿色按钮过大过亮与蓝色调不协调、空状态居中偏下
- **伴播模式分析结论**：**比例严重失衡**（右配置区不足 250px），建议 6:4 或 5:5 比例
- 分析覆盖了整个页面（含中控台背景），不是仅面板

### Boss 确认面板加宽
- Boss 问"长度加长"是否指横向宽度 → 确认是同一概念
- 执行：520→640px（仅改 1 行 `width: 640`）
- 部署成功，截图发飞书

### qwen-vision 踩坑
- 默认 timeout 60s 超时 → 改为 180s 后成功
- 本地图片 49-65KB（sips -Z 800 缩小）在 180s timeout 下约 30-60s 返回
- 需要恢复 timeout 或永久修改 skill 配置

## 22:31-22:41 四个伴播模块内容迁移

### 需求
- Boss 要求把方案 A 的四个伴播模块（形象库/音色/智能搭话/声控切屏）实际功能内容迁移到方案 B CanvasPanel 伴播模式的右侧配置区
- **关键约束**：内容照搬，布局适配 360px 宽度（右配置区 = 640-280）

### 四个模块内容
1. **形象库（AvatarLibraryPanel）**：形象网格+详情预览+造型切换+默认状态+动作分支+ChromaKey抠绿+客服弹窗+关联提醒
2. **音色（VoiceLibraryPanel）**：锁定音色卡片+6音色列表+筛选+播放试听+自定义声音训练
3. **智能搭话（AutoChatPanel）**：开关+覆盖场景+特别交代textarea+频率+TTS输出+固定搭话（触发词→回复+造型/动作绑定）
4. **声控切屏（VoiceSwitchPanel）**：开关+触发模式+关键词+商品↔造型绑定表+弹品模式+弹出次数

### 执行
- 子 agent 超时（5分钟未完成），手动写入 CanvasPanel.tsx
- 四个子组件：`AvatarContent` / `VoiceContent` / `AutoChatContent` / `VoiceSwitchContent`
- 每个组件只渲染内容区（不要顶栏/TabBar/底部操作栏）
- 原面板 520px 布局适配到 360px（网格缩窄/卡片紧凑化/两列改上下堆叠/select 缩小）
- 构建成功（401KB bundle），部署到 miaobo-b

### 验收
- 四个 Tab 截图验证全部通过
- 形象库：左侧 130px 网格 + 右侧 9:16 预览+造型+状态+动作
- 音色：锁定卡片+6音色列表+训练按钮
- 智能搭话：开关+场景+textarea+频率+固定搭话编辑
- 声控切屏：开关+模式+商品绑定表+弹品+弹出次数
- 四张截图发飞书

### 文件状态
- `CanvasPanel.tsx`：~900 行（含 4 个子组件+助播/伴播双模式）
- 其他文件未修改（AvatarLibraryPanel.tsx 等旧文件保留未动）

## 23:37 Boss 要求存档（当前）
- Boss 要求存档全部产品原型设计细节
- 确保下个会话能记住所有细节和当前进度
