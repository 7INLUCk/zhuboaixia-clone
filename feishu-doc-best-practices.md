# 飞书文档排版上限指南

## 一、可用 Block 类型及视觉效果

### 1. 基础文本块
- **Paragraph (2)**：普通段落，支持多行内元素
- **Heading1-9 (3-11)**：最多9级标题，支持折叠
- **Bullet (12)**：无序列表
- **Ordered (13)**：有序列表
- **Todo (14)**：任务列表（可分配负责人、截止日期）
- **Quote (15)**：引用块（左侧竖线 + 灰色背景，适合标注关键结论）

### 2. 高级块
- **Code (18)**：代码块，支持语言高亮（Python/JS/JSON等）
- **Callout (19)**：高亮块（最有视觉冲击力的块类型）
  - 支持 emoji 图标
  - 支持自定义背景色、边框色、文字色
  - 内部可嵌套段落和分割线
- **Table (31)**：表格，支持合并单元格、自定义列宽
- **Divider (22)**：水平分割线
- **Diagram (23)**：流程图/UML图
- **Image (27)**：图片块
- **File (28)**：文件附件
- **Sheet (34)**：内嵌电子表格
- **Bitable (35)**：内嵌多维表格（数据表/看板视图）

### 3. 特殊块
- **View (24)**：卡片视图/预览视图
- **Grid + GridColumn**：分栏布局
- **Iframe**：内嵌网页
- **Mindnote**：思维导图
- **Poll**：投票

## 二、颜色系统

### 字体颜色 (FontColor)
| 颜色 | RGB值 | 效果 |
|:---|:---|:---|
| 红色 | rgb(216, 57, 49) | 强调/警告 |
| 橙色 | rgb(222, 120, 2) | 注意 |
| 黄色 | rgb(220, 155, 4) | 提示 |
| 绿色 | rgb(46, 161, 33) | 成功/确认 |
| 蓝色 | rgb(36, 91, 219) | 链接/主色 |
| 紫色 | rgb(100, 37, 208) | 特殊标记 |
| 灰色 | rgb(143, 149, 158) | 辅助/次要信息 |

### 字体背景色 (FontBackgroundColor)
浅色系（7种）：浅粉红、浅橙、浅黄、浅绿、浅蓝、浅紫、浅灰
深色系（7种）：暗粉红、暗橙、暗黄、暗绿、暗蓝、暗紫、暗灰

### 高亮块背景色 (CalloutBackgroundColor)
浅色系（7种）：浅红、浅橙、浅黄、浅绿、浅蓝、浅紫、浅灰
深色系（7种）：暗红、暗橙、暗黄、暗绿、暗蓝、暗紫、暗灰
共14种颜色

### 高亮块边框色 (CalloutBorderColor)
7种：红、橙、黄、绿、蓝、紫、灰（色系与背景色一致）

## 三、最佳排版模式

### 模式1：引言式开头
用 Quote Block 写一句核心结论（加粗），用于文档开头做摘要。效果：左侧灰色竖线 + 大号加粗文字。

### 模式2：信息卡片
用 Callout Block 做"信息卡片"：
- 红色 callout + ❗ emoji → 警告/关键决策
- 绿色 callout + ✅ emoji → 已完成/确认项
- 蓝色 callout + 💡 emoji → 提示/建议
- 黄色 callout + ⚠️ emoji → 注意事项

### 模式3：数据对比表
用 Table Block + 列内加粗+颜色做数据对比。表头用加粗，关键数据用颜色高亮。

### 模式4：代码级技术说明
用 Code Block 写数据结构/API 说明，比纯文本更专业。

### 模式5：分节用分割线
章节之间插入 Divider Block（水平线），视觉上明确分段。

## 四、让文档"看起来很牛逼"的技巧

1. **开头用 Quote Block 做"一句话总结"**：读者看到第一句就知道核心结论
2. **关键数据用表格而非列表**：表格的结构化感知 > 列表
3. **决策用 Callout Block**：红色高亮块 + 加粗文字 = 不可忽视
4. **数据结构用 Code Block**：JSON/Python 语法高亮 = 技术感
5. **章节用 Divider 分割**：避免大段文字堆积
6. **加粗关键短语**：不要全段加粗，只加粗核心词
7. **每个章节开头一句引导语**：用 Quote Block 或加粗文字
8. **用 @文档/@人员** 交叉引用其他文档，构建知识网络
9. **多级标题折叠**：长文档用折叠标题，用户按需展开
10. **混合格式**：加粗+斜体+颜色叠加，突出层次

## 五、Alex V2 Pipeline 架构分析

### 架构概述
```
Writer (Kimi K2.5) → Markdown PRD (14KB)
    ↓
Formatter (MiniMax M2.5) → JSON 操作序列 (95个操作)
    ↓
Orchestrator → 飞书 API 逐个执行操作
```

### 优点
- **模块解耦**：每个阶段独立优化，Writer 专注内容质量，Formatter 专注结构转换
- **模型选型合理**：Kimi K2.5 写作能力强，MiniMax M2.5 做结构化转换成本低
- **操作序列可审计**：Formatter 输出 JSON，可以预检查/人工审核后执行

### 缺点
- **Orchestrator 是单点故障**：本次就是 Orchestrator 崩溃导致整个流程失败
- **95个操作 = 95次 API 调用**：效率低，任何一个失败都可能导致后续操作丢失上下文
- **Formatter 容易丢失格式信息**：从 Markdown 转 JSON 操作，Callout/颜色等高级格式容易丢失
- **错误恢复困难**：某一步失败后不知道重试哪个操作

### 改进建议
1. **Batch API**：把95个操作合并为 batch 请求，减少网络往返
2. **Checkpoint 机制**：每10个操作存一个 checkpoint，崩溃后从 checkpoint 恢复
3. **格式保真**：Formatter 需要输出 Callout/Table/Code 等高级 Block，不仅仅是文本段落
4. **直接写入**：米迦的方案（直接用 feishu_doc write）更简洁，跳过 Formatter 阶段
5. **内容与格式分离**：Writer 输出结构化内容（含格式标注），Formatter 只做 Block 映射
