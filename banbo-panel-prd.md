```markdown
# 伴播面板产品设计文档

## 1. 这个面板是什么

运营人员在直播带货时，需要让虚拟形象（数字人）在介绍特定商品时自动出场、旋转展示、自动退场。这个面板就是用来建立"商品-形象-口令"的绑定关系。

**典型使用场景：**
- 运营点击左列的"夏季连衣裙"，右列显示当前绑定的虚拟模特"小美"
- 运营在入场口令框输入"有请我们的模特上场"，在退场口令框输入"谢谢模特，请先休息"
- 主播在直播间说"有请我们的模特上场"，虚拟形象从画面右侧滑入，做展示动作
- 主播说"看看1号链接的模特上身效果"，画面直接切到形象展示特写
- 主播说"谢谢模特，请先休息"，形象鞠躬后从左侧退出

[此处插入：整体界面截图，展示左列商品列表和右列配置区同时存在的状态]

## 2. 整体布局

面板固定宽度，不可横向拉伸。高度自适应窗口，最小高度600px。

**层级结构：**
- **顶栏**：标题"伴播面板" + 关闭按钮（X）
- **主体区分栏**：
  - **左列**：固定宽度300px，商品列表区，带右侧分割线
  - **右列**：自适应剩余宽度，伴播配置区
- **弹窗层**：模态遮罩，覆盖整个面板

[此处插入：布局结构示意图，标注各区域尺寸和边界]

## 3. 左列详解

### 3.1 顶部信息区
- **标题**："直播间商品"，16px中等字重，黑色
- **统计行**：紧跟标题下方，灰色小字，格式为"12个商品 5个已绑定"
- **操作按钮组**：
  - "全部商品"：文字按钮，点击后左列显示所有商品（默认状态）
  - "去绑定"：主按钮样式（蓝色填充），点击打开全量形象选择弹窗

**边缘情况：**
- 当商品数为0时，统计行显示"暂无商品"，按钮组隐藏，列表区显示空态插图"请先在直播伴侣添加商品"

[此处插入：左列顶部信息区截图，展示统计行和按钮]

### 3.2 商品列表
每行高度72px，底部1px灰色分割线。

**单行结构（从左到右）：**
1. **商品缩略图**：48x48px，8px圆角，垂直居中
2. **文本信息区**：
   - 商品名称：单行显示，超出用"..."省略，最大宽度180px
   - 商品ID：灰色小字，格式"ID: 12345678"
3. **操作按钮**：
   - 未绑定状态：显示"去绑定"文字按钮（蓝色）
   - 已绑定状态：显示"已绑定"标签（灰色背景+灰色文字，不可点击）

**选中态样式：**
- 整行背景变为浅蓝色（#F0F7FF）
- 左侧增加3px宽蓝色竖线（#1890FF）
- 文字颜色保持黑色不变

**点击行为：**
- 点击整行（除按钮外）：右列切换显示该商品的伴播配置
- 点击"去绑定"按钮：打开形象选择弹窗（单商品模式），当前商品数据带入弹窗

**滚动行为：**
- 列表超出300px高度时出现垂直滚动条
- 滚动时顶部信息区固定，仅列表内容滚动

[此处插入：商品列表截图，展示未绑定、已绑定、选中三种状态]

## 4. 右列详解

右列内边距24px，分为四个模块垂直排列。

### 4.1 形象信息栏
高度80px，圆角卡片样式。

**未绑定状态：**
- 左侧显示灰色虚线圆圈（直径48px）+ 衣服图标emoji 👗
- 右侧文字："未绑定伴播形象，请先选择"
- 右侧按钮："选择形象"（蓝色主按钮）

**已绑定状态：**
- 左侧：形象缩略图（56x56px，圆角8px，形象为3:4比例，显示上半身）
- 中间信息：
  - 形象名称：如"小美-夏日清爽版"，16px黑色
  - 音色等级：标签形式，显示"S级"或"A级"等，对应不同颜色（S级金色，A级蓝色）
- 右侧："更换"文字按钮（灰色，hover变蓝）

**点击行为：**
- 点击"选择形象"或"更换"：打开形象选择弹窗（单商品模式）

[此处插入：形象信息栏的两种状态对比图]

### 4.2 视频预览窗口
宽度240px，高度426px（9:16比例），居中显示在右列，背景深灰色（#1A1A1A）。

**三种展示态：**

1. **默认展示态**：
   - 形象处于站立待机动作（呼吸、微动）
   - 左上角半透明标签："默认展示"（白色文字，黑色半透明底）

2. **入场展示态**：
   - 形象执行入场动画（从右侧滑入+淡入）
   - 左上角标签变为："入场中"（蓝色文字，蓝色半透明底）
   - 动画循环播放，直到点击"取消预览"

3. **退场展示态**：
   - 形象执行退场动画（鞠躬+向左侧退出+淡出）
   - 左上角标签变为："退场中"（橙色文字，橙色半透明底）
   - 动画循环播放，直到点击"取消预览"

**无形象状态：**
- 显示虚线圆圈（直径80px，2px灰色虚线）
- 中间显示emoji 🎭
- 下方文字："暂无形象，请先绑定"

**预览控制：**
- 窗口下方有两个文字按钮："预览入场" | "预览退场"
- 点击"预览入场"：切换到入场态，按钮文字变为"取消预览"（红色）
- 点击"预览退场"：切换到退场态，按钮文字变为"取消预览"（红色）
- 点击"取消预览"：回到默认态，按钮恢复原始文字
- **互斥逻辑**：同一时间只能预览一种状态，点击另一个预览按钮自动取消当前预览

[此处插入：视频预览窗口的三种状态截图]

### 4.3 输出到直播伴侣
位于预览窗口下方，居中对齐。

**开关样式：**
- 默认关闭：灰色圆角矩形背景，文字"输出到直播伴侣"灰色，左侧有显示器图标
- 开启状态：浅蓝色背景（#E6F7FF），文字变蓝，右侧出现白色✓图标

**交互细节：**
- 点击整行切换开关状态
- Hover时（无论开关状态）：显示Tooltip弹层，说明"通过NDI将伴播画面输出到直播伴侣的素材库"
- 开启后，预览窗口右上角显示红色小圆点（#FF4D4F），表示正在推流

[此处插入：输出开关的关闭、开启、Hover三种状态]

### 4.4 技能点配置
四个配置项垂直排列，每项之间24px间距。

**通用结构：**
- 顶部：彩色标签 + 配置项名称 + 红色感叹号（如有） + 操作按钮
- 中部：灰色说明文字（12px）
- 底部：输入框

**具体配置项：**

1. **入场技能（蓝色标签#1890FF）**
   - 标签文字："入场"
   - 红色感叹号：必配项标记，hover显示Tooltip"必须配置入场口令，否则形象无法触发出现"
   - 操作按钮："预览"（点击后预览窗口播放入场动画，同4.2节）
   - 说明："形象进入直播画面的触发口令"
   - 输入框：默认文案"有请模特上场"，placeholder"输入触发口令，建议4-8个字"

2. **退场技能（橙色标签#FA8C16）**
   - 标签文字："退场"
   - 红色感叹号：必配项标记，hover显示Tooltip"必须配置退场口令，否则形象无法自动退出"
   - 操作按钮："预览"
   - 说明："形象退出直播画面的触发口令"
   - 输入框：默认文案"请模特先下场"

3. **直切技能（黄色标签#FAAD14）**
   - 标签文字："直切"
   - 无红色感叹号
   - 说明："直接切换到形象展示画面的触发口令，{N}会自动替换为商品链接号"
   - 输入框：默认文案"看看{N}号链接的模特上身效果"
   - **特殊逻辑**：输入框失去焦点时，检查是否包含"{N}"，如无则自动追加在末尾（用户可手动删除）

4. **展示技能（灰色标签#8C8C8C）**
   - 标签文字："展示"
   - 无输入框，显示只读文本："自动触发（形象入场后自动循环展示动作，无需口令）"
   - 背景为浅灰色（#F5F5F5），表示不可编辑

**输入框通用规则：**
- 高度32px，圆角4px，边框#D9D9D9
- Focus状态：边框变蓝，出现蓝色外发光
- 字数限制：20个汉字，超出无法输入
- 实时显示字数："12/20"，位于输入框右下角，接近上限时变橙色，超出时变红色（但无法超出）

[此处插入：技能点配置区的完整截图，展示四个配置项]

## 5. 形象选择弹窗

弹窗宽度640px，高度自适应（最大高度600px，超出内部滚动），垂直水平居中。

**两种打开模式：**
- **单商品模式**：从商品行或右列"更换"按钮打开，标题"选择伴播形象"，保存后仅影响当前选中商品
- **全量模式**：从顶部"去绑定"按钮打开，标题"批量绑定伴播形象"，保存后影响所有未绑定商品（已绑定的不变）

**弹窗结构：**

**标题栏：**
- 左侧：标题文字（根据模式变化）
- 右侧：关闭按钮（X）

**形象网格：**
- 4列布局，每项间距16px
- 每个形象卡片：
  - 图片区域：3:4比例，圆角8px，默认显示形象封面图
  - 名称：单行省略，位于图片下方8px处
  - Radio选中态：卡片右上角显示蓝色圆形选中标记（白色✓），选中时卡片边框变蓝（2px），整体有蓝色阴影

**选中逻辑：**
- 单商品模式：单选，点击即选中，再次点击取消选中（可取消选择，表示解绑）
- 全量模式：单选，必须选中一个才能点击保存，不能取消选中（只能换选）

**底部按钮栏：**
- 左侧："取消"（灰色文字按钮）
- 右侧："保存"（蓝色主按钮，未选中形象时禁用状态，文字变为"请选择"）
- 按钮间距12px

**边缘情况：**
- 形象加载失败：显示占位图（灰色背景+裂开的图片图标）
- 形象列表为空：显示"暂无可用形象，请联系管理员添加"
- 搜索功能（如有）：标题栏下方增加搜索框，实时过滤网格内容

[此处插入：形象选择弹窗的单商品模式截图，展示选中态]

[此处插入：形象选择弹窗的全量模式截图]

## 6. 交互状态机

**预览状态流转：**

```
默认展示态 
  ↓ 点击"预览入场"
入场展示态 ←→ 点击"取消预览" 
  ↓ 点击"预览退场"（自动取消入场）
退场展示态 ←→ 点击"取消预览"
  ↓ 点击"预览入场"（自动取消退场）
入场展示态
```

**商品切换重置：**
- 当用户在左列点击其他商品行时，右列预览窗口强制重置为"默认展示态"
- 如果之前正在预览入场/退场，按钮文字恢复，动画停止
- 输出开关状态保持（不因切换商品而关闭）

**形象绑定实时生效：**
- 在弹窗中点击保存后，左列对应商品行的"去绑定"立即变为"已绑定"
- 右列形象信息栏立即更新为新形象
- 视频预览窗口立即加载新形象模型（显示加载动画，2-3秒后显示形象）

**输入框防抖：**
- 技能点输入框输入时，0.5秒防抖后自动保存（显示"已保存"绿色提示，2秒消失）
- 输入框Focus时，如果为默认文案，自动全选，方便用户直接替换

**异常状态：**
- 网络断开：尝试保存时，输入框下方显示红色文字"网络异常，请重试"，保存按钮可再次点击
- 形象加载失败：视频预览窗口显示"形象加载失败，请重新选择"，并提供"重试"按钮

[此处插入：状态流转示意图]

## 7. 常见问题

**Q：如果主播说了入场口令，但形象已经在场了怎么办？**
A：系统识别到形象已处于入场状态，忽略该口令，不重复触发入场动画。

**Q：商品ID里的{N}在直切口令中如何替换？**
A：系统自动取当前商品的链接序号（如商品在直播间是3号链接，{N}替换为"3"）。如果商品未上架（无链接号），播放到该处时{TTS}朗读"看看号链接的模特上身效果"（跳过数字）。

**Q：能否一个形象绑定多个商品？**
A：可以。左列不同商品行可以绑定同一个形象，各自保存独立的口令配置。

**Q：全量绑定时，如果部分商品已绑定形象，会被覆盖吗？**
A：不会。全量绑定仅影响"未绑定"状态的商品，已绑定的保持原样。如需覆盖，需要逐个商品重新选择。

**Q：预览窗口的尺寸为什么是240x426？**
A：这是为了匹配直播伴侣中常用的竖版形象展示区域，9:16比例确保运营看到的与主播直播间呈现的一致。

**Q：红色感叹号的必配项未配置时，能开启"输出到直播伴侣"吗？**
A：可以开启，但形象不会被触发。系统会在开启时Toast提示"入场和退场口令未配置，形象不会响应口令"。

[此处插入：异常状态提示的截图示例]

## 8. 设计决策记录

**1. 为什么左列商品行点击整行切换，而不是只有点击名称才切换？**
- 运营需要快速切换商品查看配置，整行点击增大热区，减少误操作。按钮单独处理是为了区分"查看配置"和"修改绑定"两个意图。

**2. 为什么预览状态在切换商品时强制重置？**
- 避免运营在A商品配置了形象A并预览入场，切换到B商品（绑定形象B）时，预览窗口还显示形象A的入场动画，造成认知混乱。

**3. 为什么直切口令默认包含{N}？**
- 根据运营调研，90%的直切场景需要报出商品链接号，作为默认值减少输入成本，同时允许用户手动删除应对特殊话术。

**4. 为什么全量绑定不能解绑？**
- 批量解绑风险高，容易误操作导致直播事故。如需解绑，必须逐个商品操作，增加确认成本。

**5. 为什么输出开关Hover才显示NDI说明？**
- NDI是技术术语，对初级运营可能造成困惑。常规使用无需理解NDI，只有遇到推流问题时才需要查看说明，因此采用HoverReveal减少界面信息噪音。

**6. 为什么形象选择用Radio而不是点击卡片直接选中？**
- 明确传达"单选"心智，避免用户误以为可以多选批量绑定不同形象。Radio的视觉反馈更明确。

[此处插入：关键设计决策的界面标注图]
```