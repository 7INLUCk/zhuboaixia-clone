# MONITORING-SOP.md

## 网页 Agent 监控 SOP（v1）

### 目标
- 对网页 Agent 执行过程做稳定监控。
- 一旦完成，第一时间回报 Boss。

### 固定参数
- 轮询间隔：8-12 秒状态轮询（以结构化状态读取为主）
- 完成确认：单轮命中“完成态”即可确认完成
- 截图策略：默认不做每轮截图推送，仅在关键节点截图（启动/完成/异常）

### 执行流程
1. **发送指令**给网页 Agent。
2. **T+0 立即截图一次**（作为“监控已启动”证据）。
3. 进入循环：每 **8 秒**截图一次并判断状态。
4. 判断规则：
   - 若截图显示“已完成态”（如任务进度 completed / 结果报告已生成 / 思考态消失）→ 立即判定完成；
   - 否则继续下一轮 8 秒截图。
5. 命中完成后：
   - **先短报**：已完成 + 当前结论；
   - 再补充详细摘要（改动文件、风险、下一步）。

### 强约束
- 未产生 T+0 首图，不得声称“监控已启动”。
- 未读取最新截图，不得声称“已完成”。
- 发指令后，监控优先级最高，不做无关操作。

### 回报模板
- 监控中：`已发出，监控已启动（T+0截图完成），每8秒轮询中。`
- 每轮截图：每8秒主动发送一张截图给Boss（T+0/T+8s/T+16s...）
- 完成短报：`已完成，关键结论：...`
- 完成详报：`改动清单 + 验收结果 + 风险/回滚`