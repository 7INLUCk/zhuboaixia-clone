# MiiMii 开发协作+排坑清单

> 来源：Alex 经验同步 | 2026-03-04

---

## 1. 协作流程（固定）

- **设计稿来源**：Stitch PNG（视觉）+ HTML（样式数值）
- **执行链路**：Boss描述意图 → 先转成结构化精确提示词 → 再让模型改前端 → 截图回看迭代
- **原则**：复杂页面分步做（结构→样式→数据→交互），不要一口气全改

---

## 2. 给模型下指令的关键技巧

- HTML可参考样式，但不要照搬DOM结构
- 指令必须具体到"文件+className+从什么改成什么"
- 不给模糊描述，不让模型自由发挥
- 遇到问题先排查再修复：先让模型回传 API返回值/className/DOM层级，不要盲改

---

## 3. 已踩坑（高频）

| 问题 | 解决方案 |
|------|----------|
| 视频URL相对路径 | 前端要拼 `https://assets.miimii.ai/` |
| CSP限制 | next.config.js 要配置 `media-src` 允许 assets.miimii.ai |
| 双滚动容器冲突 | layout/page 只能保留一个 `overflow-y-auto` |
| 全局导航冲突 | 全屏页走独立路由组（如 fullscreen）隐藏全局Header/Tab |
| 视频封面 | 用 `<video preload="metadata">` 提首帧，不用用户原图 |

---

## 4. 浏览器协作坑（OpenClaw）

- **多Agent共用浏览器会抢tab**：固定 targetId，减少无必要导航
- **路由验收看日志**：必须命中 `dispatching to agent (session=agent:<id>:main)`，而不是"看起来能聊"
- **中间态文本泄露**：优先做"单账号定向处理"，避免全局开关影响其他bot

---

## 5. 审查标准（每次截图都做）

- 与设计稿逐项对比：比例/间距/颜色/圆角/阴影
- 检查溢出、截断、错位、隐藏内容
- 尺寸先算后说（按目标机型实像素）
- 交互可用性必须实测（播放、滚动、点击）

---

## 6. 浏览器实操细节（进入→指令→监控→验收）

### 一、进入与准备
1. 进入后台：https://dashboard.miimii.ai/vibe-coding
2. 登录后先确认当前项目分支/任务上下文正确，再开始输入指令
3. 每次改动前先写"目标一句话"：本轮只改什么、不改什么

### 二、指令输入方式（关键）
1. 先给结构化指令，不要自然语言散聊
2. 指令模板：
   - 目标页面/文件
   - 具体改动点（精确到 className/组件）
   - 约束（参考设计稿样式，不照抄DOM）
   - 验收标准（截图看哪几项）
3. 复杂需求拆成4轮：结构→样式→数据→交互

### 三、过程监控（避免模型瞎改）
1. 先看模型是否"按你指的文件改"，不是就立刻打断重来
2. 遇到异常先排查不盲修：
   - 要模型回传 API 返回值
   - 打印关键 className/DOM 层级
   - 明确"先给排查信息，不要自行修复"
3. 每轮完成后立即截图对比设计稿，不累计到最后一起验

### 四、验收清单（每次照这个过）
- 视觉：比例/间距/颜色/圆角/阴影
- 布局：是否溢出/截断/错位
- 交互：播放/滚动/点击是否真可用
- 数据：口径是否一致（同卡片多数字必须同源）

---

## 7. 使用方式

遇到 MiiMii 相关页面改造任务时，按此清单执行：

1. 先确认设计稿（PNG + HTML数值）
2. 拆分任务（结构→样式→数据→交互）
3. 给出精确指令（文件+className+具体改动）
4. 截图验收，逐项对比
5. 发现问题先排查（API返回值/className/DOM层级），再修复